{
  "name": "tilesmith-action",
  "version": "1.0.0",
  "description": "GitHub Action for TileSmith game-tile quality control",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "esbuild src/main.mjs --bundle --platform=node --format=esm --outfile=dist/index.mjs --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\"",
    "test": "node --test",
    "lint": "eslint .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0"
  }
}
